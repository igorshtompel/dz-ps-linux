Значение: /usr/bin/bash.

Команда: 

$ echo $SHELL
/usr/bin/bash

P.S. можно также посмотреть Shell по умолчанию, например, в файле /etc/passwd:

$cat /etc/passwd
max:x:1005:1006::/home/max:/usr/bin/bash
